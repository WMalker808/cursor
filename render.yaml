services:
  - type: web
    name: guardian-style-guide
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn name_server:app --bind 0.0.0.0:$PORT --timeout 120
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
